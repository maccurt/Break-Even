export interface FormInputOptions {
  min: number;
  max: number;
  labelId: string;
  labelTitle: string;
  labelText: string;
  labelFor: string;
  inputId: string;
  inputFormControlName: string;
  placeholder: string;
  errorMessage: string;
  maxlength: number;
  decimals: number;
  showLabel: boolean;  
}
